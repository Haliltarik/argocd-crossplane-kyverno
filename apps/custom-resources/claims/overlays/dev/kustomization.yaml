apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      kind: BucketClaim
      name: bucket-claim-base
    patch: |-
      - op: replace
        path: /metadata/name
        value: test-crossplane-bucket-dev-xrd-407083967603-tamt
      - op: replace
        path: /spec/bucketName
        value: test-crossplane-bucket-dev-xrd-407083967603-tamt
      - op: add
        path: /spec/tags
        value:
          - key: Environment
            value: dev
          - key: Owner
            value: fac
          - key: Application
            value: Crossplane
      - op: add
        path: /spec/requireSSL
        value: false
      - op: add
        path: /spec/encryption
        value: false